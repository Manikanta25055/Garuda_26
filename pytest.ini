[pytest]
testpaths = tests
markers =
    unit: fast, isolated unit tests with no I/O
    integration: tests that hit the full FastAPI app via test client
    slow: tests with sleep or heavy concurrency (excluded from quick runs)
addopts = -q --tb=short
