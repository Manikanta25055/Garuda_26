// ContentView.swift — Replaced by RootView in GarudaApp.swift
// Kept as stub to avoid broken Xcode file reference. Not used directly.
import SwiftUI

struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
